// packages/worker/tests/sample.test.ts
describe('Worker Tests', () => {
  it('should have a placeholder test that passes', () => {
    // This test ensures Jest finds at least one valid test file.
    // You can replace this with real tests for your worker logic.
    expect(true).toBe(true);
  });
});
